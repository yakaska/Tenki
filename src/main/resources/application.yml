spring:
  security:
    user:
      name: user
      password: user
  datasource:
    url: jdbc:postgresql://localhost:5432/tenki
    username: postgres
    password: postgres
    driverClassName: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always

api-key: 12d10bef38432936b4fd94ee49e55a86
